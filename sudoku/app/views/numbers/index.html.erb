<p id="notice"><%= notice %></p>

<h1 style="color: purple;">3*3 SUDOKU</h1>
<p>Rule: Each row or each column has to contain exactly one 1, one 2 and one 3.</p>

<%# <table>
  <thead>
    <tr>
      <th>Index</th>
      <th>Value</th>
      <th colspan="3"></th>
    </tr>
  </thead> %>

<div id = "sudo">
    <% @numbers.each do |number| %>
      <div class="grid-container">
        <%# <td class='index'><%= number.index</td> %>
        <% @value = number.value %>
        <div class="grid-item"><%= link_to "#{@value}", edit_number_path(number)%></div>
        <!-- <td><%= link_to 'Show', number %></td> -->
        <%# <td><%= link_to 'Edit', edit_number_path(number)</td> %>
        <%#<td><%= link_to 'Destroy', number, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </div>
    <% end%>
</div>

<%# </table> %>

<br>

<%#<%= link_to 'New Number', new_number_path %>

<button class='btn'><%= link_to 'Validate', numbers_validate_path, method: :post %></button>
 